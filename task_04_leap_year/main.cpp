
#include <iostream>
int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    long long y;
    if (!(std::cin >> y)) return 0;
   int rok;
    if (rok/400==0&& rok/4==0)
        { cout<<"YES">> ;}
        else{ cout<<"NO">> ;}
        return 0;
}
